---
app: 
  headless: True
  hide_ui: True
  livestream: 2 # 0: no livestream / 1 old / 2 web rtc client
  enable_cameras: True 
  device: "cuda:${oc.decode:${oc.env:CUDA_DEVICE}}"
  active_gpu: ${oc.decode:${oc.env:CUDA_DEVICE}}
  physics_gpu: ${oc.decode:${oc.env:CUDA_DEVICE}}
  multi_gpu: False
  anti_aliasing: False
